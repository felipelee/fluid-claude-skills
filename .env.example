# One pair per company: FLUID_{SLUG}_ID and FLUID_{SLUG}_KEY
# SLUG = uppercase company name, spaces → underscore, special chars stripped
# KEY must be a PartnerToken (PT- prefix) with company admin role
FLUID_YOURCOMPANY_ID=your_company_id
FLUID_YOURCOMPANY_KEY=PT-your_partner_token
